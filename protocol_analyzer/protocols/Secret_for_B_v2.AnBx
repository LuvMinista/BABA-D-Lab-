Protocol: Secret_for_B_v2 AnB

Types: 
	Agent A,B;
	Number Msg,N;
	Function [Agent -> PublicKey] pk

Knowledge: 
	A: A,B,inv(pk(A)),pk;
	B: A,B,inv(pk(B)),pk
	
Actions:
	B -> A: {A,B,N}pk(A) 
	A -> B: {B,N,Msg}pk(B)
	
Goals:
	A ->* B: Msg
	Msg secret between A,B # fail
    B authenticates A on Msg
    B authenticates A on N