Protocol: KeyEx5 AnB
# 5th version: the correction of NSshk suggested by Denning-Sacco
# Problem: same role confusion as in version 3

Types:
	Agent A,B,s;
	Number NA,NB;
	SymmetricKey KAB;
	Function [Number -> Number] pre;
	Function [Agent,Agent ->* SymmetricKey] shk

Knowledge:
	A: A,B,s;
	B: B,A,s;
	s: A,B,s;
	A,s share shk(A,s);
	B,s share shk(B,s)

Actions:
	# B creates NB
	B -> A: A,B,NB
	A -> s: A,B,NA,NB
	# s creates key KAB
	s -> A: {|KAB,B,NA|}shk(A,s), {|KAB,A,NB|}shk(B,s)
	A -> B: {|KAB,A,NB|}shk(B,s)

Goals:
	A authenticates s on KAB,B
	B authenticates s on KAB,A
	KAB secret between A,B,s

