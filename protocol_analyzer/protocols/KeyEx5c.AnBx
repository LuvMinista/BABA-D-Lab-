Protocol: KeyEx5c AnB
# 5th version: the correction of NSshk suggested by Denning-Sacco
# and with a fix for the role confusion problem

Types:
	Agent A,B,s;
	Number NA,NB;
	SymmetricKey KAB;
	Function [Number -> Number] pre;
	Function [Agent,Agent ->* SymmetricKey] shk

Knowledge:
	A: A,B,s,pre;
	B: B,A,s,pre;
	s: A,B,s,pre;
	A,s share shk(A,s);
	B,s share shk(B,s)
	where A!=B

Actions:
	# B creates NB
	B -> A: A,B,NB
	A -> s: A,B,NA,NB
	# s creates key KAB
	s -> A: {|KAB,A,B,NA|}shk(A,s),{|KAB,A,B,NB|}shk(B,s)
	A -> B: {|KAB,A,B,NB|}shk(B,s)

Goals:
	A authenticates s on KAB,B
	B authenticates s on KAB,A
	KAB secret between A,B,s

